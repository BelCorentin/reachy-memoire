"""Memoire hub: caregiver dashboard + remote-presence API.

Runs inside the conversation-app process (wired by scripts/launch_patched.py)
on its own port so it can be tunnelled independently of the upstream UI.
"""
